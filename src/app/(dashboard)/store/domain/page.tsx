import PageStub from '../../_components/PageStub';

export default function DomainPage() {
  return (
    <PageStub
      title="Domain"
      description="Connect a custom domain or update your store URL."
    />
  );
}