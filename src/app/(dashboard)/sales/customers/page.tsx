import PageStub from '../../_components/PageStub';

export default function CustomersPage() {
  return (
    <PageStub
      title="Customers"
      description="Your customer directory, profiles, and order history."
    />
  );
}