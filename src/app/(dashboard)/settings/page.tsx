import PageStub from '../_components/PageStub';

export default function SettingsPage() {
  return (
    <PageStub
      title="Settings"
      description="Workspace, team, and role settings."
    />
  );
}